# RAVAGER SSRF Agent v2.0.0 — Dependencies
# Install: pip install -r requirements.txt
# System install: pip install -e .  then use: rage --help

httpx[http2]>=0.27.0
PyYAML>=6.0.1
rich>=13.7.1
Flask==3.0.3
requests==2.31.0
dnslib==0.9.24
dnspython==2.6.1
tldextract==5.1.2

# Optional: pip install playwright && playwright install chromium
# playwright>=1.44.0
# Optional: pip install dnspython
